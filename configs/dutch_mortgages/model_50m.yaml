# SPDX-License-Identifier: Apache-2.0
# Credit-FM 50M — stretch config; only honest if supplementary public data is added
# (e.g. Freddie Mac single-family). Do NOT use on synthetic-only data.
hidden_size: 320
num_heads: 8
profile_encoder: {num_layers: 3}
event_encoder:   {num_layers: 5}
history_encoder: {num_layers: 6}
max_events: 24
max_seq_length: 512
