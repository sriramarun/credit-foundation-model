# Reference Implementation — invoice financing

Exact commands to reproduce the published results.

```bash
bash reference_implementations/invoice_financing/train.sh
bash reference_implementations/invoice_financing/evaluate.sh
```

Configs live in `configs/invoice_financing/`; the pretrained checkpoint ships under
`models/invoice_financing_30m/` (Git LFS).
